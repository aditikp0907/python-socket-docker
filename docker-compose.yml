services:
  server:
    build: .
    command: python server.py
    volumes:
      - .:/app
    working_dir: /app

  client:
    build: .
    command: python client.py
    depends_on:
      - server
    volumes:
      - .:/app
    working_dir: /app
